import { Stack, Typography } from '@mui/material';
import React from 'react';

export default function PersonalInfo() {
  return (
    <Stack width="500px">
      <Typography variant="body1">
        Neste portifólio está a minha jornada de aprendizado como desenvolvedora full stack.
      </Typography>
      <Typography variant="body1">
        As principais stacks que eu aprendi estão listadas e você pode usá-las como filtros.
      </Typography>
    </Stack>
  );
}
